<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" type="text/css" href="<%= url("/css.css")%>">
  <title>soccer star</title>
</head>
<body>

<br></br>

<div id="ball"></div>
<h1>Goals on goals</h1>
<h5>too many to keep track of? not anymore</h5>

<div class="flash">
  <% flash.keys.each do |type| %>
    <div data-alert class="flash <%= type %> alert-box radius">
      <%= flash[type] %>
      <a href="#" class="close">&times;</a>
    </div>
  <% end %>
</div> <!-- flash -->

  <%= yield %>

</body>
</html>